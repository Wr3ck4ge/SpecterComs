# Generated by Cargo
# will have compiled files and executables
/target/
/gen/schemas
# FFmpeg shared DLLs — copied from vcpkg on the build machine, not stored in git
/ffmpeg-dlls/
# Local machine-specific Cargo config (e.g. short target-dir for long-path workaround)
/.cargo/
